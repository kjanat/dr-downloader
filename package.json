{
	"name": "dr-downloader",
	"module": "downloader.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run downloader.ts",
		"dev": "bun --watch downloader.ts",
		"fake": "bun run downloader.ts --test",
		"postinstall": "puppeteer browsers install chrome",
		"format": "biome format --write",
		"lint": "bunx biome check",
		"lint:fix": "bunx biome check --write"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.4",
		"@types/bun": "^1.2.22",
		"@types/node": "^24.4.0"
	},
	"peerDependencies": {
		"typescript": "^5.9.2"
	},
	"dependencies": {
		"puppeteer": "^24.20.0"
	}
}
